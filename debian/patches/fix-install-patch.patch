Index: irgsh-cli-1.3.9/makefile
===================================================================
--- irgsh-cli-1.3.9.orig/makefile
+++ irgsh-cli-1.3.9/makefile
@@ -6,7 +6,7 @@ all: build
 build:
 	go build -ldflags="-s -w" -o $(BINARY)
 clean:
-	rm $(BINARY)
+	rm -f $(BINARY)
 
 install: build
 	install -D -m 0755 $(BINARY) \
